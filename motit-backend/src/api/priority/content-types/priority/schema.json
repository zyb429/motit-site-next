{
  "kind": "collectionType",
  "collectionName": "priorities",
  "info": {
    "singularName": "priority",
    "pluralName": "priorities",
    "displayName": "Priorities",
    "description": "Ticket priorities"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "unique": true,
      "maxLength": 100
    },
    "color": {
      "type": "enumeration",
      "enum": ["gray", "blue", "orange", "red"],
      "default": "gray"
    },
    "level": {
      "type": "integer",
      "default": 0,
      "min": 0,
      "max": 10
    }
  }
}